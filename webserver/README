项目说明
服务器架构：I/O单元+请求队列+逻辑单元

I/O单元：
主线程：负责网络通讯	epoll统一事件源（监听套接字、连接套接字、信号、定时器）+模拟Proactor模式处理事件

定时器：用一个核心时间轮和四个外层时间轮实现，将添加、删除、滴答均降低为O(1)级别。

请求(任务)队列：基于锁实现的线程安全队列（降低锁的粒度，提高并发度）

逻辑单元：
线程池：增加逻辑模块的cpu资源

并发模型：半同步/半反应堆

有限状态机：处理HTTP请求报文，面向对象方法重新设计

报文处理过程：
I/O单元（读取HTTP报文到进程缓冲区）-->逻辑单元（解析请求行-->解析首部行-->处理消息体-->处理请求报文-->组建响应报文进程缓冲区）-->I/O单元（发送响应报文）

重新设计了逻辑单元解析请求报文、处理请求报文、组建响应报文部分，可扩展。

实现一个日记业务：GET、POST、DELETE、PUT实现拉取文件、登录注册、删除文件、提交文件
用基本认证实现质询机制，用线程安全查找表存储用户基本信息。
